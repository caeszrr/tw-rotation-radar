# 台股輪動雷達 — 網站完整說明

> 這份文件的目的：**讓你不用打開網站，也完全知道它是什麼、畫面上每個東西代表什麼、
> 資料哪裡來、什麼時候更新、以及它明確不保證什麼。**
>
> 線上頁面：<https://caeszrr.github.io/tw-rotation-radar/>
> 計算規格與程式細節：[`README.md`](README.md)
> 本文對應版本：`TWRRG-v1`，資料至 **2026-08-14**（647 個交易日）

---

## 1. 一分鐘看懂

台股輪動雷達把台股的**產業族群**與**規模風格**畫成一張「相對輪動圖」（RRG，
Relative Rotation Graph）。它回答的問題只有一個：

> **錢正在往哪個族群流進去、從哪個族群流出來？**

畫面中央是一張四象限散布圖。每一個族群是一顆「水滴」，水滴的**頭**是最新一天的位置，
拖在後面的**尾巴**是它過去幾天走過的路徑。看尾巴的方向，就知道這個族群正在往哪裡走。

它**不是**選股工具，**不會**告訴你買什麼賣什麼。畫面上只有事實：象限、位置、轉換紀錄。

---

## 2. 這張圖在說什麼

### 2.1 兩個座標軸

| 軸 | 名稱 | 意思 |
|---|---|---|
| 橫軸 X | **RS-Ratio 相對強弱** | 這個族群相對大盤**已經**強到什麼程度 |
| 縱軸 Y | **RS-Momentum 動能** | 這個族群的相對強弱**正在**變強還是變弱 |

兩軸都以 **100 為中心**（不是 0）。100 代表「跟大盤一樣」。畫面預設顯示 94–106。

- X > 100：比大盤強　　X < 100：比大盤弱
- Y > 100：相對強弱正在改善　　Y < 100：正在惡化

### 2.2 四個象限

```
          動能 ↑
              │
    ② 改善     │     ① 領先
   IMPROVING  │   LEADING
   （轉強中）  │  （又強又還在加速）
              │
 ─────────────┼───────────── 100 ──→ 相對強弱
              │
    ③ 落後     │     ④ 弱化
   LAGGING    │   WEAKENING
   （又弱又   │  （還強但動能已經
     還在弱）  │    轉頭向下）
              │
```

**典型的輪動是順時針**：③落後 → ②改善 → ①領先 → ④弱化 → 回到③。
一個族群如果從「改善」跨進「領先」，通常代表資金真的進來了；
從「領先」掉進「弱化」，代表它還強，但動能已經先轉彎。

### 2.3 讀圖的例子

> 假設「散熱(含液冷)」的水滴頭落在 (102.4, 101.8)，尾巴從左下方一路拉上來。
>
> 讀法：它現在**比大盤強 2.4 個標準化單位**，而且**動能仍在往上**（在①領先象限），
> 尾巴由左下往右上，代表過去這幾天是一路走強進來的，不是在高檔盤整。

反過來，如果水滴頭在①領先象限但尾巴是**從右上往左下**掉，那就是正在往④弱化走。
**尾巴的方向比位置更重要。**

---

## 3. 畫面上每一個東西是什麼

### 3.1 頂部：大日期與狀態標籤

畫面最上方是一個很大的日期，右上角有一個小標籤（chip）。這兩個東西合起來
**永遠說明「你現在看到的是哪一天、那一天定稿了沒有」**。它有三種狀態：

| 狀態 | 什麼時候 | 大日期 | 小標籤 |
|---|---|---|---|
| **盤中** | 台北交易日 09:00–13:30 | `2026-08-17(一)盤中` ← **今天** | `盤中暫定 · HH:MM:SS` |
| **已收盤、未定稿** | 交易日 13:30 之後、當日收盤資料尚未產生 | 上一個定稿日，例如 `2026-08-14週五` | `08-17 已收盤，今晚更新收盤定稿`（琥珀色） |
| **定稿後** | 當日收盤資料已經進來 | `2026-08-17週一` | `最新收盤定稿` |

另外還有兩種例外標籤：

- `歷史定稿` —— 你把日期滑桿拉到過去某一天時
- `最新收盤 08-14（12 天前）` —— **資料超過 4 天沒更新**時會轉琥珀色並印出實際落後天數

> **為什麼要分這麼細**：因為「管線壞掉」和「今天沒行情」在畫面上長得一模一樣。
> 這個網站的設計原則是寧可多寫幾個字，也不要讓人以為看到的是今天的資料。

### 3.2 圖表左上角：日期徽章

圖表繪圖區的左上角有一顆半透明的小徽章，寫著當前檢視日，例如 `08-14 週五`。

它存在的唯一理由是：**手機往下捲動看圖時，頂部的大日期會捲出畫面**，
徽章讓你隨時知道正在看哪一天。播放、拖曳、動畫過程中它每一幀都跟著更新。
盤中時它會顯示 `08-17 盤中 10:32:45`。

### 3.3 水滴與尾巴

每個族群一顆水滴：

- **頭**（實心圓＋光暈）＝ 最新一天的位置
- **尾巴**（由濃到淡的漸層）＝ 過去 N 天走過的軌跡，**濃的那端是現在，淡的那端是過去**
- 尾巴長度預設 5 個交易日，可在「顯示設定」調整 3–20

滑鼠移到水滴上（手機是點）會顯示該族群那一天的實際數值；其他族群會同時淡出，
讓你專心看一條。

### 3.4 空心圓點與虛線（只有盤中才出現）

盤中時，每顆水滴頭旁邊會多出一個**空心圓點**，用虛線連著頭點，圖右下角同時出現說明：

```
實體尾巴＝前一交易日收盤定稿
○ 空心點＝今日盤中暫定，未收盤
```

**這兩件事必須分清楚**：實心的尾巴是**已定稿的官方收盤**；空心點是**今天到現在為止的暫定位置**，
它會一路移動到 13:30 收盤為止，之後凍結，當晚被正式收盤值覆蓋。

空心點的算法與限制寫在 [§7.4](#74-盤中暫定值的性質與限制)。

### 3.5 象限變化面板

圖表下方是「象限變化」面板，只列**事實**：哪個族群在哪一天從哪個象限跨到哪個象限。
兩種跨越會額外標記：

- 🟢 **資金轉入**：改善 → 領先
- 🟡 **動能翻轉**：領先 → 弱化

那一天若沒有任何（已顯示的）族群跨象限，就直接寫出來，不會留白讓人猜。

### 3.6 播放與速度

日期列有一顆 **▶ 播放**，按下去會從左到右回放整個顯示視窗，看資金輪動的過程。

旁邊那顆按鈕是**播放速度**，顯示目前檔位，點一下循環切換：

```
中（600ms/格，預設） → 快（250ms） → 慢（1000ms） → 回到中
```

你的選擇會被記住，下次打開還在。

播放時水滴不是「跳」到下一天，而是在兩個真實交易日之間**平滑滑過去**。
這是純粹的視覺補間 —— 詳見 [§7.5](#75-播放補間是視覺不是資料)。

播放中象限變化面板的行為依速度不同：

- **慢速**：面板每一格跟著刷新，標題變成 `08-14 象限變化`
- **中／快速**：整塊面板被蓋住，中央寫一行
  `象限變化逐日檢視僅於「慢速」播放時提供`
  —— 因為一格 600ms 甚至 250ms 根本讀不完，留著舊內容比蓋掉更誤導
- 按停止或拉滑桿停格 → 遮罩消失，顯示停格那一天的內容

### 3.7 顯示設定（可摺疊）

| 設定 | 選項 | 說明 |
|---|---|---|
| **分頁** | 產業族群／規模風格 | 兩組完全獨立的族群定義 |
| **視窗** | 20／60／120／240 交易日 | **只影響日期滑桿與回放範圍，不影響任何計算** |
| **權重** | 等權／市值 | 族群指數的成分股加權方式 |
| **基準** | 4 選 1，見下 | 拿誰當「大盤」來比 |
| **尾巴** | 3–20 點 | 尾巴要拖幾個交易日 |
| **族群顯示** | 逐一勾選 | 可全選／全不選，選擇會被記住 |

**四種基準**：

| 基準 | 意思 |
|---|---|
| 發行量加權股價**報酬**指數 | 上市大盤（含息） |
| 櫃買**報酬**指數 | 上櫃大盤（含息） |
| 上市＋上櫃市值併權 | 依市值 91.6% / 8.4% 併起來 |
| 全部族群等權平均 | 拿本站自己的 14 個族群平均當基準 |

> 基準一律用**報酬指數**（含息），與成分股的還原價是同一個調整基礎。
> 拿不含息的指數去比含息的個股，會系統性地讓所有族群看起來偏強。

### 3.8 頁尾

頁尾固定寫出：資料來源、基準、**已知近似**、計算時間、交易日數，
以及盤中即時層目前的更新頻率。頻率文字會跟著實際狀態改變
（正常約 13 秒；若自動降級會改寫成 60 秒模式或批距退回）。

---

## 4. 資料涵蓋什麼

### 4.1 產業族群（14 個，223 檔成分股去重）

| 族群 | 檔數 | | 族群 | 檔數 |
|---|---|---|---|---|
| 散熱(含液冷) | 20 | | 電源供應器/BBU | 25 |
| 光通訊/CPO | 19 | | 被動元件 | 17 |
| 記憶體 | 22 | | 重電 | 16 |
| PCB | 19 | | 軍工 | 21 |
| CCL銅箔基板 | 19 | | **航運** | 12 |
| 先進封裝/CoWoS | 20 | | **金融** | 12 |
| IP/ASIC設計 | 16 | | | |
| 伺服器/機殼 | 25 | | | |

**航運**與**金融**是刻意放進來的**對照錨**：它們與 AI 供應鏈幾乎無關，
如果連它們都跟著一起動，那多半是大盤在動，不是族群輪動。

### 4.2 規模風格（7 個分頁）

| 分頁 | 代表標的 |
|---|---|
| 上市大型 | 0050 |
| 上市中型 | 0051 |
| 上櫃大型 | 006201 元大富櫃50 |
| 高股息 | 0056 |
| 低波高息 | 00713 |
| 公司治理 | 00692 |
| 上櫃中小 | 富櫃200指數（**無歷史來源，逐日累積中**） |

### 4.3 時間範圍

- 資料起點 **2023-12-12**（更早期間成分股覆蓋不足會失真，刻意不畫）
- 目前到 **2026-08-14**，共 **647 個交易日**
- 各基準組合的第一個有效日皆為 **2024-02-01**（前面 43 個交易日是計算暖身期）

### 4.4 資料來源（全部公開、零費用、零 token）

| 用途 | 端點 |
|---|---|
| 上市個股/ETF 收盤 | `openapi.twse.com.tw/v1/exchangeReport/STOCK_DAY_ALL` |
| 上櫃個股/ETF 收盤 | `www.tpex.org.tw/openapi/v1/tpex_mainboard_daily_close_quotes` |
| 加權股價報酬指數 | `openapi.twse.com.tw/v1/indicesReport/MI_INDEX` |
| 櫃買報酬指數 | `www.tpex.org.tw/openapi/v1/tpex_reward_index` |
| 除權息預告表 | `openapi.twse.com.tw/v1/exchangeReport/TWT48U_ALL` |
| 官方端點故障時的備援 | FinMind（免費層） |
| 盤中即時報價 | 證交所 MIS 公開端點（約 5 秒一次快照） |

---

## 5. 計算規格（摘要）

完整推導與程式碼對應見 [`README.md`](README.md)。重點：

```python
RS          = 100 * (族群指數 / 基準指數)        # 兩者皆為還原價 / 報酬指數基礎
RS_Ratio    = 100 + (RS - RS.rolling(14).mean()) / RS.rolling(14).std(ddof=1)
ROC         = 100 * (RS_Ratio / RS_Ratio.shift(10) - 1)
RS_Momentum = 100 + (ROC - ROC.rolling(14).mean()) / ROC.rolling(14).std(ddof=1)
```

- `n = 14`（雙滾動視窗）、`k = 10`（ROC 回看）、標準差用**樣本**（ddof=1）、置中於 **100**
- 族群指數 ＝ 等權（或市值權）**日報酬鏈結**，基期 100；
  成分股必須在 `t` 與 `t-1` 都有價才納入當日，**絕不以跨日報酬冒充單日報酬**
- 最低歷史 43 個交易日；**不足 60 交易日的族群一律拒繪並標「歷史不足」**

> RRG 的官方 JdK 演算法是營業秘密，公開實作在三個參數上彼此不一致
> （樣本／母體標準差、動能置中 100 或 101、ROC 用滾動位移或固定基準）。
> 本專案一次鎖死這三個選擇並用單元測試凍結，**不含任何可調自由參數**。
> 也就是說：本站的數值不保證與其他 RRG 工具相同，但保證自己前後一致。

---

## 6. 什麼時候更新

### 6.1 收盤定稿（每個交易日一次）

```
台北 17:50  排程觸發（主跑）
台北 19:30  補跑（若主跑已完成則冪等結束，不重複寫入）
            ↓
        抓官方收盤 → 完整性閘門 → 重算 → 發布靜態頁
```

> **誠實提醒**：GitHub 的排程實測會**嚴重延遲**。2026-08-13 那一跑實際 20:06 才起跑、
> 20:14 才寫入；2026-08-14 是 20:02 起跑。
> **請以畫面上「大日期變成今天」為準，不要盯著時鐘等。**

### 6.2 盤中即時層（交易日 09:00–13:30）

- 每輪掃描全部 231 個代號（229 檔個股/ETF ＋ 2 條指數），分 3 批、批距 4 秒
- 實測**每輪 10.5–11.1 秒**；網頁在你**正在看**的時候每 10 秒取一次
- 你切到別的分頁 → 自動降到 60 秒；**非盤中完全不發任何請求**
- 13:30 之後空心點凍結，當晚被收盤定稿覆蓋

---

## 7. 這個網站保證什麼、不保證什麼

這一節是整份文件最重要的部分。

### 7.1 保證：拒繪優於造假

**資料不完整就不發布，絕不發半套。**

每天的資料要通過**完整性閘門**才會上線：當日必須取得 **231 個代號中的 95% 以上**，
且兩條基準報酬指數不得缺漏。不合格就**保留上一版**，畫面停在昨天，
並發出失敗告警 —— 而不是畫出一張缺一半資料的圖。

### 7.2 保證：出事的時候畫面會說出來

| 狀況 | 畫面會怎麼說 |
|---|---|
| 資料超過 4 天沒更新 | 標籤轉琥珀色，印出 `最新收盤 08-14（12 天前）` |
| 盤中即時層連不上 | `即時層離線，以下為 HH:MM:SS 資料` |
| 即時層自動降級 | 標籤加註 `（60 秒模式）`，頁尾文字同步改掉 |
| 抓取批距自動放寬 | 頁尾改印 `約 13 秒（批距已自動退回 5 秒）` |
| 今天已收盤但還沒定稿 | `08-17 已收盤，今晚更新收盤定稿` |

**設計原則**：任何「悄悄變慢」或「悄悄停住」都必須在畫面上留下痕跡。
系統絕不在退化之後還掛著原本的招牌。

### 7.3 已知近似（不是 bug，是刻意標註的限制）

- **還原價僅處理現金股利**；股票股利與減資未還原
- **市值權重採當期股數快照**（2026-08-11），歷史增減資未還原
- 族群指數起算日不早於 **2023-12-12**
- **「上櫃中小」（富櫃200指數）無歷史來源**，自建置日起逐日累積，
  滿 60 交易日前一律標「資料累積中」不畫點
- 官方端點故障時會改由 **FinMind 補齊**。上線前以真實資料比對過四種欄位對映
  （上市個股、上櫃個股、加權報酬、櫃買報酬），10 項全對齊、0 不一致；
  **但完整性閘門不因為降級而放寬**

### 7.4 盤中暫定值的性質與限制

空心點**不是**用完整公式重算的結果。它是一個刻意簡化的近似：

```
今日暫定 RS-Ratio = 昨日 RS-Ratio × (1 + 族群當日報酬) / (1 + 基準當日報酬)
今日暫定 動能     = 昨日動能（原封不動）
```

也就是說：

- 空心點**只會左右移動，不會上下移動** —— 縱軸（動能）在盤中維持昨天的值
- 它是「相對強弱大概往哪邊偏」的即時感覺，**不是**當日收盤後會得到的正式座標
- 收盤後正式計算的位置**會與空心點不同**，這是預期行為，不是誤差

**鐵律：盤中暫定值永遠不會寫進歷史資料。** 它只存在於你當下看的那個畫面。

### 7.5 播放補間是視覺，不是資料

播放時水滴在兩個交易日之間平滑移動 —— **中間那些位置全部是視覺內插，不是資料**。

- 資料點**只有官方收盤**，本站**絕不製造日內假點**
- 補間只存在於畫布的繪製層；hover 讀到的永遠是真實交易日與真實數值
- 這件事有自動化驗收在把關：補間全程，資料層的座標**逐位元等於**該真實交易日的值

### 7.6 效能會自動退讓

畫面繪製有兩段護欄：連續 3 幀超過 16.67ms（60fps 預算）→ 先讓尾巴改用較低取樣點數；
若仍然慢 → 關掉播放補間，回到直接跳格。**寧可少一個動畫，不要掉幀。**

實測：桌機每幀 p95 **2.4ms**、手機 **2.1ms**。

### 7.7 不保證什麼

- **不保證與其他 RRG 工具的數值相同**（見 §5）
- **不保證排程準時**（見 §6.1）
- **不保證預測任何未來走勢** —— 本站只呈現已經發生的相對強弱與動能
- **不含任何買賣建議**

---

## 8. 系統怎麼運作（給想知道的人）

```
每個交易日傍晚
  GitHub Actions ──→ 抓 4 個官方端點（打不通則 FinMind 備援）
                 ──→ 完整性閘門（231 檔 / 95%）
                 ──→ 重算 history.json ──→ commit 進 docs/ ──→ GitHub Pages 發布

盤中 09:00–13:30
  Cloudflare Worker（cron 每分鐘點火）
                 ──→ Durable Object 用 alarm 自己排下一輪（每 10-11 秒）
                 ──→ 抓證交所 MIS 快照 ──→ 寫進 DO（KV 每分鐘一份保底）
  瀏覽器 ──→ 每 10 秒讀一次 /latest，自己在前端算暫定位置

監控
  Healthchecks 心跳（盤中每分鐘、日線每次成功）
  失敗／降級 ──→ Telegram
  畫面標示（就算所有告警管道都斷了，看畫面仍看得出來）
```

前端是一頁靜態 HTML：Plotly 負責象限、座標軸、hover 與標籤；
水滴與尾巴畫在一層絕對定位的 `<canvas>` 上（perfect-freehand 單筆畫 + 沿路徑漸層）。

**營運成本：NT$0。** 全部使用 GitHub、Cloudflare、Healthchecks、FinMind 的免費層。
盤中即時層一天用掉 Cloudflare 免費額度約 4.5%。

---

## 9. 法規姿態

盤中即時層抓的是證交所 MIS 公開端點。台灣法規以「延遲 ≥20 分鐘」為分界，
即時轉發的授權是法人 vendor 制。

**本專案的姿態**：只給極少數人（2–10 位）、免登入、輸出的是**計算後的指標**而非原始報價表
—— 風險低但非零。**使用者一旦超過十幾人或想公開分享，必須改成延遲 ≥20 分鐘或關掉即時層。**

關掉的方法是刪掉頁面裡的一行 `window.RADAR_LIVE_URL=...`，
之後頁面退回純收盤定稿模式，零網路請求。

---

## 10. 免責

> **本頁僅呈現市場公開資料之計算結果，非投資建議。**

頁面只呈現象限、轉換與排名等事實，不含任何買賣指令用語。
所有已知的近似與限制都寫在 §7 與頁尾，沒有隱藏項目。

---

## 附錄：詞彙表

| 詞 | 意思 |
|---|---|
| **RRG** | Relative Rotation Graph，相對輪動圖 |
| **RS-Ratio** | 相對強弱，橫軸，100 = 與基準同步 |
| **RS-Momentum** | 相對強弱的變化速度，縱軸，100 = 不快不慢 |
| **領先／改善／落後／弱化** | 四象限，見 §2.2 |
| **定稿** | 官方收盤資料已寫入、不會再變動 |
| **暫定** | 盤中即時推估，會一直變，收盤後被覆蓋 |
| **還原價** | 把除息造成的價格斷點補回來的價格 |
| **報酬指數** | 含息的指數，與還原價同一調整基礎 |
| **完整性閘門** | 資料達不到門檻就拒絕發布的機制 |
| **對照錨** | 刻意放進來、與主題無關的族群（航運、金融） |
